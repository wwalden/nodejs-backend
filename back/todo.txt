TODO for CORS Issue

Jeudi
- Ligne à ajouter dans la CORS Policy
    res.setHeader('Access-Control-Allow-Credentials', true);
    full text (app.use((req, res, next) => { res.setHeader('Access-Control-Allow-Origin', process.env.SECRET_ORIGIN); res.setHeader('Access-Control-Allow-Headers', 'Origin, X-Requested-With, Content, Accept, Content-Type, Authorization'); res.setHeader('Access-Control-Allow-Methods', 'GET, POST, PUT, DELETE, PATCH, OPTIONS'); res.setHeader('Access-Control-Allow-Credentials', true); next(); });)
- ReInstall correctement le projet en followant les guidelines (body Parser non nécessaire)
- Message sur Workplace ?


Vendredi
- dotenv? l'ajouter anyway... (dans le cours ou pas?)
- lire les docs du fichier découpage
